# 02 — Beta

**What to build:** Write the beta marker file. Same shape as 01 with a
disjoint write-surface, so the two can be ground in parallel.

**Blocked by:** None

**Write-surface:** `src/beta.txt`

**Status:** ready-for-agent

- [ ] `src/beta.txt` exists and contains `beta`.
