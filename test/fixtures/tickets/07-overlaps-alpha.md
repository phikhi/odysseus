# 07 — Overlaps alpha

**What to build:** Shares a write-surface with 01, so the two must be
sequenced rather than run in parallel.

**Blocked by:** None

**Write-surface:** `src/alpha.txt`, `src/eta.txt`

**Status:** ready-for-agent

- [ ] `src/alpha.txt` gains a second line.
- [ ] `src/eta.txt` exists.
