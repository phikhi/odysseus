# 01 — Alpha

**What to build:** Write the alpha marker file. The baseline frontier ticket:
open, unblocked, ready-for-agent, unclaimed, lowest NN.

**Blocked by:** None

**Write-surface:** `src/alpha.txt`

**Status:** ready-for-agent

- [ ] `src/alpha.txt` exists and contains `alpha`.
