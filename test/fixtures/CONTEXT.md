# Demo project

Fixture context for the harness: a fresh session is handed a pointer to this
file, so it has to exist and be readable inside the throwaway project.

## Language

**Marker**: a one-line text file under `src/` proving a ticket ran.
